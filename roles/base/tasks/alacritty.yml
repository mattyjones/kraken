---

# TODO: better comments
# TODO: need to set it as the default terminal program in the GUI

##-------------------------- Terminal ----------------------##

- name: Install alacritty
  ansible.builtin.apt:
    name: alacritty
    state: latest
  become: true

# TODO: need to set the directory/user dynamically based on other tasks
- name: Copy the configuration file
  copy:
    src: alacritty.yml
    dest: /home/matt/.alacritty.yml

# TODO: firacode needs to be referenced as a dependency to ensure it is already installed and if not install it