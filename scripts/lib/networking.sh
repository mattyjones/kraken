#!/bin/bash

install_firefox() {
  local pkgs=()"firefox"
}

install_chrome() {
  local pkgs=()"chrome"
}

install_openconnect() {
local pkg=("openconnect")
}

# configure firefox
# configure chrome
# can I get te vpn file from htb via api
